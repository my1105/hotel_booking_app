<h1>施設一覧</h1>

<% if current_user %>
  <%= link_to "施設を登録する", new_room_path %>
<% end %>

<hr>

<% @rooms.each do |room| %>
  <div>
    <h3><%= link_to room.name, room_path(room) %></h3>
    <p><%= room.description %></p>
    <p>¥<%= room.price %></p>
  </div>
  <hr>
<% end %>